import regrouping = require('../src/regroupingProblems');

describe('testing generateRandomEquations', () => {
  test('testing generation of 1 equation', () => {
    expect(1).toBe(1);
    //expect(regrouping.generateRandomEquations(1)).toBe();
  });
});